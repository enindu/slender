<div class="navbar">
  <div class="navbar-content">
    <button id="sidebar" class="btn btn-square">
      <i class="fa fa-bars"></i>
    </button>
  </div>
  <a href="{{ "/admin"|page }}" class="navbar-brand">
    <img src="{{ "/images/logo.svg"|asset("admin") }}" alt="{{ app.name }}" width="30" height="30" loading="lazy"> {{ app.name|title }}
  </a>
  <span class="navbar-text">{{ app.version }}</span>
</div>
